---
type NavItem = {
  href: string;
  label: string;
  current?: boolean;
};

interface Props {
  title: string;
  subtitle?: string;
  backHref?: string;
  backLabel?: string;
  navItems?: NavItem[];
}

const {
  title,
  subtitle,
  backHref,
  backLabel = '返回',
  navItems = [],
} = Astro.props;

const hasActions = Astro.slots.has('actions');
---

<header class="admin-nav-shell border-b">
  <div class="container mx-auto px-4 py-4 flex flex-col gap-4 md:flex-row md:items-center md:justify-between">
    <div class="min-w-0">
      <div class="flex flex-wrap items-center gap-3">
        {backHref && (
          <a
            href={backHref}
            class="nav-action nav-outline inline-flex items-center rounded-md border px-3 py-1.5 text-sm transition-colors"
          >
            &larr; {backLabel}
          </a>
        )}
        <div class="min-w-0">
          <h1 class="text-lg font-bold">{title}</h1>
          {subtitle && <p class="nav-subtitle mt-1 text-sm">{subtitle}</p>}
        </div>
      </div>

      {navItems.length > 0 && (
        <nav class="mt-3 flex flex-wrap items-center gap-2" aria-label="后台导航">
          {navItems.map((item) => (
            <a
              href={item.href}
              class:list={[
                'nav-pill inline-flex items-center rounded-full px-3 py-1.5 text-sm transition-colors',
                item.current && 'is-current',
              ]}
              aria-current={item.current ? 'page' : undefined}
            >
              {item.label}
            </a>
          ))}
        </nav>
      )}
    </div>

    {hasActions && (
      <div class="flex flex-wrap items-center gap-3 md:justify-end">
        <slot name="actions" />
      </div>
    )}
  </div>
</header>
