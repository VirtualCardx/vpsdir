---
interface Props {
  variant?: 'success' | 'error';
  class?: string;
}

const {
  variant = 'success',
  class: className = '',
} = Astro.props;
---

<div class:list={['admin-alert', variant === 'success' ? 'admin-alert-success' : 'admin-alert-error', className]}>
  <slot />
</div>
